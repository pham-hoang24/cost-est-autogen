mode: standalone
auth:
  rootUser: admin
  rootPassword: changeme # Replace via external secret manager in production
defaultBuckets: "datasets,artifacts,audit"
ingress:
  enabled: true
  hostname: minio.local
  tls: true
resources:
  requests:
    cpu: 200m
    memory: 512Mi


